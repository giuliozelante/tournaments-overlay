query SetsQuery($eventId:ID!,$page:Int,$perPage:Int){event(id:$eventId){id name sets(page:$page,perPage:$perPage,sortType:CALL_ORDER){nodes{id fullRoundText round state startAt winnerId slots{entrant{id name}standing{placement stats{score{value}}}}}}}}