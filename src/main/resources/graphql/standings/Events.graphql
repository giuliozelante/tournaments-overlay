query EventsQuery($tournamentId: ID!) {
  tournament(id: $tournamentId) {
    events {
      id
      name
      slug
      numEntrants
      startAt
      videogame {
        name
      }
    }
  }
} 